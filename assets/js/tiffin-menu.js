document.addEventListener('DOMContentLoaded',async()=>{const mount=document.getElementById('tiffinMenu');if(!mount||!window.CsvAPI)return;const safe=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));try{const rows=(await CsvAPI.loadAllPublic('data/tiffin-menu.csv')).items.filter(x=>String(x.status).toLowerCase()==='active').sort((a,b)=>Number(a.sort_order||99)-Number(b.sort_order||99));mount.innerHTML=rows.length?rows.map(x=>`<article><h3>${safe(x.name)}</h3><strong>Rs ${safe(x.price)}</strong><p>${safe(x.description)}</p><a href="menu-item.html?slug=${encodeURIComponent(x.slug)}">View dish details</a></article>`).join(''):'<p>Menu is being updated. Please call to order.</p>'}catch(e){mount.innerHTML='<p>Menu temporarily unavailable. Please call to order.</p>'}});
