document.addEventListener('DOMContentLoaded',async()=>{const mount=document.getElementById('tiffinMenu');if(!mount||!window.CsvAPI)return;const safe=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));try{const rows=(await CsvAPI.loadAllPublic('data/tiffin-menu.csv')).items.filter(x=>String(x.status).toLowerCase()==='active').sort((a,b)=>Number(a.sort_order||99)-Number(b.sort_order||99));mount.innerHTML=rows.length?rows.map(x=>{const detail='menu-item.html?slug='+encodeURIComponent(x.slug);const message=encodeURIComponent('Hello Call4All, I would like to order '+x.name+' at Rs '+x.price+' in Kukas, Jaipur.');return `<article><h3>${safe(x.name)}</h3><strong>Rs ${safe(x.price)}</strong><p>${safe(x.description)}</p><div class="tiffin-menu-actions"><a class="btn btn-accent btn-sm" href="tiffin-center-kukas.html#book">Book Now</a><a class="btn btn-outline-dark btn-sm" href="${detail}">View Details</a><a class="btn btn-whatsapp btn-sm" href="https://wa.me/917737353588?text=${message}" target="_blank" rel="noopener">WhatsApp</a></div></article>`}).join(''):'<p>Menu is being updated. Please call to order.</p>'}catch(e){mount.innerHTML='<p>Menu temporarily unavailable. Please call to order.</p>'}});
